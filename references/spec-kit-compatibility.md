# Spec Kit compatibility and provenance

Official project inspected: GitHub `github/spec-kit`, commit `d4229c071c7ea3885b43e8a7739847300f618f13` (18.09.2026). The official feature-spec template was read; this packet supplies original, project-specific filled artifacts and helper templates using the conventional directory layout. [G01, G02]

Included: `.specify/memory/constitution.md`, `.specify/templates/`, `.specify/scripts/bash/check-prerequisites.sh`, `specs/001-ai-podcast-player/{spec,plan,tasks,research,data-model,quickstart}.md`, contracts and checklists. The helper script is original and read-only, not advertised as an unmodified upstream script.

Not included: the full specify-cli distribution, its Python dependencies, agent plugin installers, external app code or a Git checkout. No network is needed to read or validate this packet. Installing the official CLI is optional and separate. Existing agent installations determine actual command names.

The original project-specific prompts are not official upstream commands. They can be pasted directly into any capable coding agent. A full upstream archive download was not available through the session's download route; no partial file is presented as that archive.
